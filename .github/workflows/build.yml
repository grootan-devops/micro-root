name: Build · Manual Image Verification

"on":
  workflow_dispatch:

run-name: "CI · ${{ github.event_name }} · ${{ github.sha }}"

concurrency:
  group: build-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  init:
    uses: grootan-devops/github-ci-library/.github/workflows/init.yml@dev
    secrets: inherit
    permissions:
      contents: read
      actions: read
      pull-requests: read
    with:
      ignore-docker: "false"
      ignore-chart: "true"

  trivy-cache:
    uses: grootan-devops/github-ci-library/.github/workflows/trivy-cache.yml@dev
    secrets: inherit
    permissions:
      contents: read
      actions: write
    with:
      enable-java-db: false

  image:
    needs: init
    uses: grootan-devops/github-ci-library/.github/workflows/buildah.yml@dev
    secrets: inherit
    permissions:
      contents: read
      packages: write
    with:
      image-tag: ${{ needs.init.outputs.image-push-tag }}
      image-repository: ${{ needs.init.outputs.image-push-repository }}
      base-image-repo: registry.access.redhat.com/ubi9/ubi-micro
      base-image-tag: "9.8-1784702951"
      install-packages: "bash coreutils-single basesystem"
      required-packages: "bash coreutils-single basesystem"
      container-entrypoint: "/usr/bin/dumb-init --"
      container-cmd: "bash"

  image-test:
    needs: image
    runs-on: ${{ vars.CI_RUNNER || 'ubuntu-26.04' }}
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - name: Log in to Docker Hub
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v3
        with:
          registry: registry-1.docker.io
          username: ${{ secrets.IMAGE_REGISTRY_USERNAME }}
          password: ${{ secrets.IMAGE_REGISTRY_PASSWORD }}
      - name: Run image smoke test as non-root
        env:
          IMAGE_REF: ${{ needs.image.outputs.image-ref-digest }}
        run: >-
          docker run --rm --privileged --user 10001:10001 --entrypoint /bin/bash
          -v "${PWD}:/tmp/image-test:ro" "${IMAGE_REF}"
          -c 'cd /tmp/image-test && ./ci_image_test.sh'

  image-scan:
    needs: [image, image-test, trivy-cache]
    uses: grootan-devops/github-ci-library/.github/workflows/scan.yml@dev
    secrets: inherit
    permissions:
      contents: read
      checks: write
    with:
      scan-type: image
      image-ref: ${{ needs.image.outputs.image-ref-digest }}
      fail-on-warnings: true
